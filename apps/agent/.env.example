PORT=3001

# Where the control plane's WebSocket gateway lives
API_URL=http://localhost:3000

# This node's identity/token (issued by the control plane when you register a node)
NODE_ID=
AGENT_TOKEN=
